# T-002 — Navigation shell

**Status**: backlog
**Type**: feat

---

## Goal

Build the persistent application shell: a collapsible sidebar with module navigation, a top bar, and the root layout that wraps all pages.

---

## Acceptance criteria

- [ ] Root `layout.tsx` includes sidebar + main content area
- [ ] Sidebar lists links to: Dashboard, Journal, Tasks, Projects, Habits, Calendar
- [ ] Active route is highlighted using the accent token
- [ ] Sidebar is collapsible on desktop (icon-only collapsed state)
- [ ] Sidebar collapses to a bottom navigation bar on mobile
- [ ] Sidebar collapse state persisted in Zustand store (provider pattern)
- [ ] ThemeToggle component present in the shell (from T-001)
- [ ] All components follow file structure rules (index.tsx / index.styles.ts / index.test.tsx / index.stories.tsx)
- [ ] All tests green
- [ ] `npm run build` clean
- [ ] `npm run lint` clean
