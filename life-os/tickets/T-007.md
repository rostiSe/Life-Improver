# T-007 — Habit log & streak

**Status**: backlog
**Type**: feat

---

## Goal

Add the ability to log habit completions and display streak data. The log button is the primary daily interaction.

---

## Acceptance criteria

- [ ] On the `/habits` page, each habit has a "Log" button
- [ ] Clicking Log creates a `habit_log` row via Server Action
- [ ] Multiple logs per day allowed up to `targetCount`
- [ ] Today's completion count shown on each habit card (e.g. "2 / 3")
- [ ] Current streak calculated and displayed (consecutive days/weeks/months with at least one log)
- [ ] Streak calculation is a pure utility function with unit tests
- [ ] Log history accessible per habit (last 30 entries)
- [ ] All components follow file structure rules
- [ ] All tests green
- [ ] `npm run build` clean
- [ ] `npm run lint` clean
